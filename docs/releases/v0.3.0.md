# v0.3.0 — SPZ import

- **Tag:** `v0.3.0`
- **Commit:** `80fad96743640777a1fa9aa13e7fae48ee4c2a1e`
- **Date:** 2026-07-20
- **Status:** draft release assembled; awaiting human publication review

## Objective

Prove the shared architecture supports a second, compressed format without
special-casing it. SPZ import is the deliverable; the architectural claim is
that a new format reaches USD through the same `GaussianCloudData` →
`GaussianLayerWriter` path PLY uses, contributing no format-specific USD
authoring.

## What shipped

Matches the finalized `## [0.3.0]` section of
[CHANGELOG.md](../../CHANGELOG.md); no further entries are expected.

- **SPZ container reader** (`plugins/gaussian-spz/src/io/SpzReader.*`).
  Versions 1-3, gzip-framed, implemented from Niantic's MIT-licensed
  specification rather than by vendoring the reference implementation.
  miniz 3.0.2 vendored at `third_party/miniz` because the runtime ships no
  zlib; gzip framing is parsed in-repo. `CanRead()` is a bounded inflate of
  the magic only.
- **Semantic decoder** (`GaussianSpzDecoder`). Dequantization per
  [SPZ_MAPPING.md](../reference/SPZ_MAPPING.md), the RUB→RDF reference-frame
  conversion (position and quaternion-vector negation plus per-coefficient SH
  band sign flips), both rotation encodings, and SH degrees 0-3.
- **USD integration.** The SPZ plugin routes decoded data through the shared
  `libs/gaussian-usd` writer; PLY and SPZ author an identical hierarchy,
  schema, and metadata.
- **Stable `GSPZ-****` diagnostics** with a machine-readable catalog,
  cross-checked against the source constants in both directions. Malformed,
  unsupported, and internal failures stay distinct: v4 and SH degree 4 are
  *unsupported*, not corrupt.
- **Real-asset corpus.** Two CC0-1.0 author-captured Scaniverse exports
  (`yashica-t4`, `leica-sofort`), 8,192 Gaussians each, derived by
  `scripts/spz_subset.py` with recorded provenance.
- **Performance baselines** for SPZ recorded in
  [PERFORMANCE_BASELINES.md](../reference/PERFORMANCE_BASELINES.md), measured
  through the same seam as PLY. SPZ imports roughly 4x faster per Gaussian; no
  architectural regression appeared.
- **PLY/SPZ cross-format equivalence tests**
  ([EQUIVALENCE.md](../reference/EQUIVALENCE.md)). One source model defined in
  shared-model space is encoded into both formats and decoded back through the
  two independent decoders, then compared at tolerances derived from the SPZ
  quantization steps rather than tuned to pass. This is the check that holds
  the RUB→RDF conversion, the 15 SH band sign flips, and the two SH memory
  layouts to an independent witness — a formula that is wrong consistently on
  both the encode and decode side survives a per-format inverse check but not
  this one.
- **CI coverage.** Three `gaussian-spz` source cells mirroring the PLY ones,
  moving the PR and release lanes together.

## Compatibility

- SPZ container versions 1, 2, and 3. Version 4 (ZSTD, per-attribute streams)
  is rejected with a specific unsupported-version diagnostic, not a
  malformed-container one, and is deferred to v0.5.0.
- SH degrees 0-3. Degree 4 is rejected as unsupported (the shared model carries
  0-3).
- Read-only. No SPZ writing or export.
- PLY behaviour from v0.2.0 is unchanged; `GSPLY-****` codes are neither
  renumbered nor reused.

## Verification observed

Hosted: all 6 release cells green on
[run 29741754978](https://github.com/animu-sphere/usd-3dgs-plugins/actions/runs/29741754978)
— 3 × `gaussian-ply` and 3 × `gaussian-spz`, Windows at L4, macOS and Linux at
L5, each proving packaging digest-reproducible within the run. The
`workflow_dispatch` dry run on the same tree
([run 29741537174](https://github.com/animu-sphere/usd-3dgs-plugins/actions/runs/29741537174))
passed first, per the gate. The PR lane passed the same 6 cells on
[PR #19](https://github.com/animu-sphere/usd-3dgs-plugins/pull/19).

Local results observed 2026-07-20 at the tagged tree:

- Local `ctest` (plain root composition): 8/8 green — `gaussianCore_unit`,
  `gaussianUsd_writer_unit`, `gaussianPly_decoder_unit`,
  `gaussianPly_python_smoke`, `gaussianSpz_reader_unit`,
  `gaussianSpz_decoder_unit`, `gaussianSpz_python_smoke`,
  `gaussian_ply_spz_equivalence`.
- `ost plugin test --workspace --up-to 5`: 2 bundles, 0 failed;
  `gaussian-spz` OK (12 pass, 0 fail, 3 skip) with L5 golden roundtrip
  matching.
- `ost plugin package` + `--from-package --up-to 5` for both bundles: OK
  (14 pass, 0 fail, 1 skip — the skip is the package-origin L5 item below).
- `ost ci validate`: 6 cells, structure OK.
- `scripts/release.py guard --tag v0.3.0`: release gate passed.

Note that the local Windows archive digests differ from the published ones
(`c3585c74…` / `e87e1de7…` locally against `5a586542…` / `5af3860e…`). That is
expected: the local runtime resolves from source `build` while the hosted cells
consume the pinned `artifact`. The gate requires reproducibility *within* a
run, which every cell proved.

## Artifacts

Per-target lean bundles with manifest and SBOM sidecars, plus a source
archive. This is the first release carrying two bundles, so each target ships
both `gaussian-ply` and `gaussian-spz`; install only the one you need, as
neither depends on the other.

SHA-256 of the bundle archives and the source archive:

```text
5a586542f7fc738b91984809e851b3778ad0125a5a994a03d1d1068d810a2dcb  gaussian-ply-0.3.0-cy2026-windows-x86_64-py313-usd.tar.zst
c8addcaa39361ceb57c489b7953b6c65dce4050559cf2acb7f6078400bc28887  gaussian-ply-0.3.0-cy2026-macos-arm64-py313-usd.tar.zst
f9c38e13ccf36a298a5535cff1e7da52914e830465554480ccbd71e7b44811d6  gaussian-ply-0.3.0-cy2026-linux-x86_64-py313-usd.tar.zst
5af3860e3f7b204a0cb9ceee3f5ee62045e22b3deb706281dde984a2c144a578  gaussian-spz-0.3.0-cy2026-windows-x86_64-py313-usd.tar.zst
4034c262bdd4a7add775cad6a0fa43dea9e06876e8110e0d6e250cffae8db7cf  gaussian-spz-0.3.0-cy2026-macos-arm64-py313-usd.tar.zst
96acb4b1dedd30a7c49ae64a83665f9e76ca17c135c8e11850b868af5d7ee9ae  gaussian-spz-0.3.0-cy2026-linux-x86_64-py313-usd.tar.zst
b80d73622fa1b48c9dc921e9950cc95e9afce74d32115eee2d2fe8aae64ad556  usd-3dgs-plugins-0.3.0-src.tar.gz
```

The published `SHA256SUMS` asset additionally covers every `manifest.json` and
`sbom.spdx.json` sidecar.

## Known limitations at this release

- **Equivalence is verified with synthetic pairs, not real assets.** One
  source model is encoded into both formats and compared; the committed
  `yashica-t4` and `leica-sofort` corpora are *not* cross-compared, because
  they are independent captures rather than two encodings of one
  reconstruction. SPZ v1's float16 position path is also deliberately unpaired
  and stays pinned by the SPZ decoder suite. Both exclusions and their reasons
  are in [EQUIVALENCE.md §5](../reference/EQUIVALENCE.md).
- **The SPZ spherical-harmonic quantization range is asymmetric**
  (`[-1.0, +0.9921875]`, from `(byte - 128) / 128`), so a coefficient of
  exactly `-1.0` carrying a negative band sign flip clamps on encode. The
  error is bounded by one quantization step and is attributable to the encoder
  that wrote the file, not to this decoder.
- **Windows stays capped at OST L4**, inherited from the hosted multiline-USDA
  line-ending finding, so the golden roundtrip gate runs only on macOS and
  Linux.
- **Package-origin L5 still skips** — upstream OST packaging seam.
- **macOS across-run package digest reproducibility is still open**; Windows
  and Linux archives are byte-identical across hosted runs.
- **The model's reference frame is PLY-native RDF while the authored stage
  declares `upAxis = "Y"`.** Correcting that is a major-version change and is
  tracked in the backlog, deliberately not fixed here.
- **Rendering is not included.** These plugins import; a Gaussian-capable
  Hydra delegate is required to see splats.

## Notes on the release itself

First release to ship two bundles, so it is also the first exercise of the
claim the cycle set out to test: a second format reached USD without any
format-specific USD authoring. The release lane moved from 3 cells to 6 by
editing `openstrata.ci.yaml` alone, because both the PR lane and
`scripts/release.py matrix` derive from it.

Two process notes worth carrying forward:

- The `gaussian-spz` bundle had never been packaged before this release, which
  is why its missing `/dist/` `.gitignore` entry only surfaced at the end of
  the cycle. Packaging a new bundle once, early, would have caught it sooner.
- The record was drafted early as `v0.3.0-draft.md` and deliberately not
  renamed until the tag existed. That kept the tag-time work to transcription
  while still honoring the rule that a record pins values only the release
  lane can produce — the tagged commit above could not have been written into
  a file that lives inside that commit.

## What comes next

v0.4.0 and beyond per [release-plan.md](../roadmap/release-plan.md); v0.5.0
carries SPZ v4 (ZSTD) and expanded format compatibility.
